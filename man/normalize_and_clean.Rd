% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MODULE_6_ORF_CODON.R
\name{normalize_and_clean}
\alias{normalize_and_clean}
\title{normalize_and_clean}
\usage{
normalize_and_clean(edf, mincount = 2, method = "average")
}
\arguments{
\item{edf}{Dataframe output of get_tr_regions()}

\item{mincount}{Minimum average of counts across all three region types. Passed to \code{\link{replace_low_count_transcripts}}.}

\item{method}{Filtering method passed to \code{\link{replace_low_count_transcripts}}. Default: "average".}
}
\value{
data frame of the normalised and filtered transcript counts for each region
}
\description{
Normalize per-region transcript read counts using the median-of-ratios method
and remove transcripts with low counts across regions. Combines \code{\link{normalize_median_of_ratios}}
and \code{\link{replace_low_count_transcripts}} into a single step, so per-sample normalization
factors do not need to be supplied or computed by hand.
}
\examples{
tr_regions_df_pivot_norm_clean <- normalize_and_clean(tr_regions_df)
}
