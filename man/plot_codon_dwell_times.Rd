% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MODULE_2_PREP_CELP.R
\name{plot_codon_dwell_times}
\alias{plot_codon_dwell_times}
\title{plot_codon_dwell_times}
\usage{
plot_codon_dwell_times(
  codon_estimates,
  highlight_codons = NULL,
  p_cutoff = 0.05,
  base_size = 30,
  label_size = 8
)
}
\arguments{
\item{codon_estimates}{Data frame produced by \code{\link{get_codon_dwell_time_estimates}} (columns codon, mean_dwell_time, p_val).}

\item{highlight_codons}{Optional character vector of codons to highlight with a distinct fill color
(e.g. the codons decoded by a tRNA of interest). Accepts either T or U notation. Default: none highlighted.}

\item{p_cutoff}{Significance threshold used to label significant bars "*". Non-significant bars are left
unlabeled. Default: 0.05.}

\item{base_size}{Base font size for the plot theme. Default: 30.}

\item{label_size}{Font size for the "*" significance labels above each bar. Default: 8.}
}
\value{
A ggplot2 bar plot object.
}
\description{
Bar plot of per-codon mean dwell time estimates produced by \code{\link{get_codon_dwell_time_estimates}}.
Codon labels are converted to RNA notation (T -> U). The start codon (AUG) is colored blue and stop
codons (UAA, UGA, UAG) are colored dark red on the axis text; bars are labeled "*" where \code{p_val}
is below \code{p_cutoff}, "NS" otherwise.
}
\details{
Per-codon axis text coloring (AUG/stop codons) uses a vector of colours in a single
\code{ggplot2::element_text()} call, which is not officially supported by ggplot2 but works correctly
with current ggplot2 versions; the resulting "Vectorized input to element_text()" warning is expected
and suppressed.
}
\examples{
plot_codon_dwell_times(codon_ests, highlight_codons = c('GGU', 'GGG', 'GGA', 'GGC'))
}
