% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MODULE_5_TER_ALT.R
\name{visualize_meta_analysis}
\alias{visualize_meta_analysis}
\title{visualize_meta_analysis}
\usage{
visualize_meta_analysis(
  validation,
  beta_title = "Effect Size Correlation",
  p_title = "P-value Correlation",
  title_size = 1.4,
  label_color = "black",
  label_size = 1.3,
  coef_color = "white",
  coef_size = 1,
  method = "color",
  col = NULL
)
}
\arguments{
\item{validation}{Output list of \code{\link{validate_meta_test}}, containing \code{beta_cor} and
\code{p_cor} (each an \code{Hmisc::rcorr} object).}

\item{beta_title}{Title for the effect size (logFC) correlogram, in bold. Default: "Effect Size Correlation".}

\item{p_title}{Title for the p-value correlogram, in bold. Default: "P-value Correlation".}

\item{title_size}{Font size (cex) for both titles. Default: 1.4.}

\item{label_color}{Colour of the row/column labels. Default: "black" (corrplot's own default is red).}

\item{label_size}{Font size (cex) for the row/column labels. Default: 1.3 (corrplot's own default, 1,
is quite small).}

\item{coef_color}{Colour of the correlation values written inside each cell. Default: "white".}

\item{coef_size}{Font size (cex) for those values. Default: 1.}

\item{method}{Cell rendering method passed to \code{corrplot::corrplot} (e.g. "color", "circle",
"number"). Default: "color".}

\item{col}{Colour palette passed to \code{corrplot::corrplot}. Default: \code{NULL} (corrplot's own
diverging blue-white-red palette).}
}
\value{
Invisibly \code{NULL}; called for its side effect of drawing the two corrplots in the current
graphics device.
}
\description{
Plot the two correlation matrices produced by \code{\link{validate_meta_test}} (effect
size and p-value agreement between the pooled fit, each split-level fit, and the meta-analysis
combination) side by side as a pair of \code{corrplot::corrplot} heatmaps, instead of one above the other.
}
\details{
Uses \code{graphics::par(mfrow = c(1, 2))} to place the two plots side by side (restoring the previous
\code{par()} settings afterward, so it doesn't affect later plots). The colour scale legend is only
drawn once, on the first (effect size) plot, since it's identical for both.
}
\examples{
validation <- Ribolog::validate_meta_test(rr.v2_dummy[, -1], Ribolog::sample_attributes_dummy, read_type ~ lung_metastasis,
                                           split_var = "cell_line_origin", term_name = "lung_metastasisY",
                                           feature_list = as.vector(rr.v2_dummy$transcript))
Ribolog::visualize_meta_analysis(validation)
Ribolog::visualize_meta_analysis(validation, beta_title = "logFC agreement",
                                  p_title = "P-value agreement", label_size = 1.5, title_size = 1.6)
}
