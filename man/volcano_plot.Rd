% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MODULE_4_TER_BASE.R
\name{volcano_plot}
\alias{volcano_plot}
\title{volcano_plot}
\usage{
volcano_plot(
  fit,
  covariate = NULL,
  x = "logFC_lung_metastasisY",
  y = "corrected_pvalue_lung_metastasisY",
  xlab = "Ln fold change",
  ylab = "-Log10 FDR",
  title = "LMCN data, metastatic vs non-metastatic",
  subtitle = NULL,
  selectLab = NULL,
  labFace = "bold",
  titleLabSize = 12,
  border = "full",
  pCutoff = 0.001,
  FCcutoff = 1.5,
  xlim = c(-5, 5),
  ylim = c(0, 10)
)
}
\arguments{
\item{fit}{Output object of a \code{\link{logit_seq}} call.}

\item{covariate}{(Optional) Name (or unambiguous prefix) of the model term to plot, e.g. "intercept" or
"lung_metastasis" - matched against the \code{logFC_<term>} columns of \code{fit} (so "lung_metastasis"
matches a term stored as "lung_metastasisY"). When given, this sets \code{x} to \code{logFC_<term>} and
\code{y} to \code{corrected_pvalue_<term>} (or \code{pvalue_<term>} if \code{fit} has no corrected p-values),
overriding \code{x}/\code{y}. Leave \code{NULL} to specify \code{x}/\code{y} manually instead.}

\item{x}{Data column denoting the log fold change. Ignored if \code{covariate} is given.}

\item{y}{Data column denoting the p-value. Ignored if \code{covariate} is given.}

\item{xlab}{X-axis label.}

\item{ylab}{Y-axis label.}

\item{title}{Plot title.}

\item{subtitle}{Plot subtitle. Default: \code{NULL} (no subtitle; EnhancedVolcano's own default would
otherwise print the italic word "EnhancedVolcano").}

\item{selectLab}{(Optional) Character vector of specific gene/transcript labels (matching
\code{rownames(fit)}) to display. Default: \code{NULL}, which lets EnhancedVolcano label every point
that passes both \code{pCutoff} and \code{FCcutoff} (i.e. the most significant hits).}

\item{labFace}{Font face for point labels. Default: "bold" (plain text is hard to see against the
point cloud); pass "plain" to disable.}

\item{titleLabSize, border}{Passed to \code{EnhancedVolcano::EnhancedVolcano}.}

\item{pCutoff}{P-value significance threshold.}

\item{FCcutoff}{Fold change significance threshold (e.g. 1.5 = 1.5x), not log fold change. \code{fit}'s
\code{logFC_<term>} column is on the natural-log scale, so this is converted internally
(\code{log(FCcutoff)}) before being passed to EnhancedVolcano.}

\item{xlim, ylim}{Axis limits.}
}
\value{
A volcano plot visualisation
}
\description{
Function to make the volcano plot from the output of \code{\link{logit_seq}}.
}
\details{
A visualisation of the volcano plot resulting from the p-values and log fold change.
EnhancedVolcano emits a few warnings that are expected and cosmetic (handling of exact-zero p-values,
and its own internal use of a deprecated ggplot2 `size` argument); these are suppressed here.
}
\examples{
fit1_FDR <- Ribolog::logit_seq(rr.v2_dummy[,-1], Ribolog::sample_attributes_dummy, read_type ~ lung_metastasis,
                                as.vector(rr.v2_dummy$transcript), adj_method = 'fdr',
                                id_mapper = Ribolog::human_id_mapper, id_column = 'gene_name')
vplot <- volcano_plot(fit1_FDR, covariate = "lung_metastasis")
vplot <- volcano_plot(fit1_FDR, covariate = "lung_metastasis", selectLab = c("CYP26B1"))
}
