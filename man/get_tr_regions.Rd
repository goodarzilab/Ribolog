% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MODULE_6_ORF_CODON.R
\name{get_tr_regions}
\alias{get_tr_regions}
\title{get_tr_regions}
\usage{
get_tr_regions(reads_psite_list)
}
\arguments{
\item{reads_psite_list}{A reads_psite_list object produced by \code{\link{psite_info_rW}}}
}
\value{
A dataframe containing the number of reads mapping to each region, for each transcript, with a
\code{sample_name} column identifying each sample - named to match the \code{sample_name} convention
used elsewhere (e.g. a sample attributes/design table), so it can be used directly as a \code{sample_ID}.
}
\description{
Function to count the number of reads mapping to each of the 3 regions per transcript per sample.
}
\examples{
tr_regions_df <- get_tr_regions(reads_psite_list)
}
