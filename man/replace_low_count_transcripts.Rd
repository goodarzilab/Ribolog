% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MODULE_6_ORF_CODON.R
\name{replace_low_count_transcripts}
\alias{replace_low_count_transcripts}
\title{replace_low_count_transcripts}
\usage{
replace_low_count_transcripts(edf, mincount = 2, method = "average")
}
\arguments{
\item{edf}{Dataframe output of get_tr_regions()}

\item{mincount}{Minimum average of counts across all three region types}

\item{method}{Filtering method passed to \code{\link{min_count_filter}}. Default: "average".}
}
\value{
data frame of the filtered transcript counts for each region
}
\description{
Function to remove transcripts with low level counts across all three regions
}
\details{
A transcript with zero observed reads in a given region across every sample has no row for that
(transcript, psite_region) combination in \code{edf} at all (see \code{\link{get_tr_regions}}), so
pivoting to wide format introduces \code{NA} there rather than 0; these are filled with 0 before
filtering, since a missing combination genuinely means zero counts, not an unknown value.
}
