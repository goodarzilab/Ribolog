% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MODULE_2_PREP_CELP.R
\name{get_codon_dwell_time_estimates}
\alias{get_codon_dwell_time_estimates}
\title{get_codon_dwell_time_estimates}
\usage{
get_codon_dwell_time_estimates(bc_pa_df, covariate_name, ref_level)
}
\arguments{
\item{bc_pa_df}{Data frame produced by \code{\link{get_codon_bias_coefficients}}, with columns codon_type,
bias_coefficient, and the covariate column named \code{covariate_name}.}

\item{covariate_name}{Name of the covariate column in \code{bc_pa_df} to test (e.g. "genotype").
Must currently have exactly two levels; one coefficient/p-value is reported per codon.}

\item{ref_level}{The level of the covariate to use as the reference/baseline (e.g. "wt").}
}
\value{
A data frame with one row per codon type: codon, mean_dwell_time (the covariate's coefficient),
and p_val, sorted by descending mean_dwell_time.
}
\description{
Calculate per-codon ribosome dwell time estimates and p-values between two levels of a
covariate, from a bc_pa_df produced by \code{\link{get_codon_bias_coefficients}}. For each codon type, fits
\code{glm(bias_coefficient ~ covariate)} across all transcripts/positions with that codon, and reports
the covariate's coefficient (the estimated dwell time shift relative to \code{ref_level}) and its p-value.
}
\examples{
codon_ests <- get_codon_dwell_time_estimates(bc_pa_df, covariate_name = "genotype", ref_level = "wt")
}
